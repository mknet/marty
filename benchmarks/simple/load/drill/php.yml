---
concurrency: 20
base: "{{ BENCH_BASE_URL }}"
iterations: 5000
rampup: 0

plan:
  - name: POST webhook (php mod_php)
    request:
      url: /php/webhook.php
      method: POST
      headers:
        Content-Type: application/json
        X-Webhook-Secret: bench-secret
      body:
        file: ../../fixtures/webhook-post.json
